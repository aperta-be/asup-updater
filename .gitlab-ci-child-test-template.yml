# Test release <TAG>-<PHP_VERSION>
test_image:<PHP_VERSION>:
  stage: test
  tags:
    - sandbox
  before_script:
    #- docker info
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  script:
    - docker run gitlab.dazzle.be:5050/aperta/asup:<DEV><TAG>-<PHP_VERSION>

